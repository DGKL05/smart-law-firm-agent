spring:
  application:
    name: dgkl
  profiles:
    active: dev

server:
  port: 8080

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: smart-law-firm-local-dev-secret-key-change-before-prod-2026
  expiration: 86400000
