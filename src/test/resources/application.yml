spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:dgkl;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  data:
    redis:
      host: localhost
      port: 6379
    mongodb:
      uri: mongodb://localhost:27017/dgkl_test

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: smart-law-firm-local-dev-secret-key-change-before-prod-2026
  expiration: 86400000
